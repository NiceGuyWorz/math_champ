# Math Champ Mobile

This is a minimal React Native app built with Expo. It connects to the backend to fetch math exercises.

## Setup

Install Expo CLI and dependencies:

```bash
npm install -g expo-cli
cd mobile
npm install
```

Run the app:

```bash
npm start
```

Make sure the Python backend is running on `localhost:8000`.
